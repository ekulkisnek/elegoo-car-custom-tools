diff --git a/selfdrive/ui/layouts/main.py b/selfdrive/ui/layouts/main.py
index 15d44e24d..62f35247e 100644
--- a/selfdrive/ui/layouts/main.py
+++ b/selfdrive/ui/layouts/main.py
@@ -1,3 +1,5 @@
+import os
+
 import pyray as rl
 from enum import IntEnum
 import cereal.messaging as messaging
@@ -42,6 +44,9 @@ class MainLayout(Widget):
     self._onboarding_window = OnboardingWindow()
     if not self._onboarding_window.completed:
       gui_app.push_widget(self._onboarding_window)
+    elif os.environ.get("OPENPILOT_START_ONROAD") == "1":
+      self._set_current_layout(MainState.ONROAD)
+      self._sidebar.set_visible(False)
 
   def _render(self, _):
     self._handle_onroad_transition()
diff --git a/selfdrive/ui/mici/layouts/main.py b/selfdrive/ui/mici/layouts/main.py
index 95258e279..975e52734 100644
--- a/selfdrive/ui/mici/layouts/main.py
+++ b/selfdrive/ui/mici/layouts/main.py
@@ -1,3 +1,5 @@
+import os
+
 import pyray as rl
 import cereal.messaging as messaging
 from openpilot.selfdrive.ui.mici.layouts.home import MiciHomeLayout
@@ -70,6 +72,8 @@ class MiciMainLayout(Scroller):
     if not self._setup:
       if self._alerts_layout.active_alerts() > 0:
         self._scroller.scroll_to(self._alerts_layout.rect.x)
+      elif os.environ.get("OPENPILOT_START_ONROAD") == "1":
+        self._scroll_to(self._onroad_layout)
       else:
         self._scroller.scroll_to(self._rect.width)
       self._setup = True
diff --git a/selfdrive/ui/mici/onroad/augmented_road_view.py b/selfdrive/ui/mici/onroad/augmented_road_view.py
index 09d5d57fb..451196284 100644
--- a/selfdrive/ui/mici/onroad/augmented_road_view.py
+++ b/selfdrive/ui/mici/onroad/augmented_road_view.py
@@ -1,3 +1,4 @@
+import os
 import time
 import numpy as np
 import pyray as rl
@@ -37,6 +38,11 @@ ROAD_CAM_MIN_SPEED = 10  # m/s (25 mph)
 CAM_Y_OFFSET = 20
 
 
+def _webcam_dev_mode() -> bool:
+  """NOBOARD / USE_WEBCAM: no comma Panda — hide misleading offroad overlay (e.g. ESP32 webcam dev)."""
+  return os.environ.get("NOBOARD") == "1" or os.environ.get("USE_WEBCAM") is not None
+
+
 class BookmarkIcon(Widget):
   PEEK_THRESHOLD = 50  # If icon peeks out this much, snap it fully visible
   FULL_VISIBLE_OFFSET = 200  # How far onscreen when fully visible
@@ -171,9 +177,9 @@ class AugmentedRoadView(CameraView):
   def _update_state(self):
     super()._update_state()
 
-    # update offroad label
+    # update offroad label (webcam/NOBOARD dev: no "system booting" — there is no Panda)
     if ui_state.panda_type == log.PandaState.PandaType.unknown:
-      self._offroad_label.set_text("system booting")
+      self._offroad_label.set_text("" if _webcam_dev_mode() else "system booting")
     else:
       self._offroad_label.set_text("start the car to\nuse openpilot")
 
@@ -244,8 +250,8 @@ class AugmentedRoadView(CameraView):
 
     self._bookmark_icon.render(self.rect)
 
-    # Draw darkened background and text if not onroad
-    if not ui_state.started:
+    # Draw darkened background and text if not onroad (skip in webcam/NOBOARD dev so MJPEG preview is unobstructed)
+    if not ui_state.started and not _webcam_dev_mode():
       rl.draw_rectangle(int(self.rect.x), int(self.rect.y), int(self.rect.width), int(self.rect.height), rl.Color(0, 0, 0, 175))
       self._offroad_label.render(self._rect)
 
diff --git a/system/manager/helpers.py b/system/manager/helpers.py
index 047d0ac2d..b565bb044 100644
--- a/system/manager/helpers.py
+++ b/system/manager/helpers.py
@@ -13,6 +13,9 @@ from openpilot.common.basedir import BASEDIR
 from openpilot.common.params import Params
 
 def unblock_stdout() -> None:
+  # On macOS, fork() after native GUI libs (e.g. raylib) are loaded is unsafe. Skip when set.
+  if os.environ.get("OPENPILOT_SKIP_UNBLOCK_STDOUT") == "1":
+    return
   # get a non-blocking stdout
   child_pid, child_pty = os.forkpty()
   if child_pid != 0:  # parent
diff --git a/system/manager/process_config.py b/system/manager/process_config.py
index 0b9918319..d7ed3ce05 100644
--- a/system/manager/process_config.py
+++ b/system/manager/process_config.py
@@ -12,6 +12,13 @@ WEBCAM = os.getenv("USE_WEBCAM") is not None
 def driverview(started: bool, params: Params, CP: car.CarParams) -> bool:
   return started or params.get_bool("IsDriverViewEnabled")
 
+
+def webcamerad_gate(started: bool, params: Params, CP: car.CarParams) -> bool:
+  """Like driverview, but optional env to always run webcamerad (e.g. dev MJPEG without toggling UI)."""
+  if os.environ.get("OPENPILOT_WEBCAM_ALWAYS") == "1":
+    return True
+  return driverview(started, params, CP)
+
 def notcar(started: bool, params: Params, CP: car.CarParams) -> bool:
   return started and CP.notCar
 
@@ -70,7 +77,7 @@ procs = [
   PythonProcess("logmessaged", "system.logmessaged", always_run),
 
   NativeProcess("camerad", "system/camerad", ["./camerad"], driverview, enabled=not WEBCAM),
-  PythonProcess("webcamerad", "tools.webcam.camerad", driverview, enabled=WEBCAM),
+  PythonProcess("webcamerad", "tools.webcam.camerad", webcamerad_gate, enabled=WEBCAM),
   PythonProcess("proclogd", "system.proclogd", only_onroad, enabled=platform.system() != "Darwin"),
   PythonProcess("journald", "system.journald", only_onroad, platform.system() != "Darwin"),
   PythonProcess("micd", "system.micd", iscar),
